<view class="revenue vertical" bindcustomevent="customEvent">
  <view class="revenue-item horizontal">
    <view class="label">日期：</view>
    <picker mode="date" value="{{selectedDate}}" bindchange="dateChange">
      <view class="value select">{{selectedDate}}</view>
    </picker>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">类型：</view>
    <picker range-key="content" value="{{revenueTypeIndex}}" range="{{revenueTypes}}" bindchange="revenueTypeChange" >
      <view class="value select">{{revenueTypes[revenueTypeIndex].content}}</view>
    </picker>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">收入：</view>
    <input placeholder-class="placeholder" class="value" type="digit" placeholder="请输入收入" bindinput="saveRevenue"></input>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">成本：</view>
    <input placeholder-class="placeholder" class="value" type="digit" placeholder="请输入成本" bindinput="saveBase"></input>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">结账类型：</view>
    <picker range-key="content" value="{{billTypeIndex}}" range="{{billTypes}}" bindchange="billTypeChange">
      <view class="value select">{{billTypes[billTypeIndex].content}}</view>
    </picker>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">是否结账：</view>
    <picker range-key="content" value="{{billCloseTypeIndex}}" range="{{billCloseTypes}}" bindchange="billCloseTypeChange">
      <view class="value select">{{billCloseTypes[billCloseTypeIndex].content}}</view>
    </picker>
  </view>
  <view class="revenue-item horizontal">
    <view class="label">备注：</view>
    <textarea placeholder-class="placeholder" class="value description" placeholder="选填(最多100个字)" maxlength="100" cursor-spacing="100rpx" bindinput="saveDescription" disabled="{{disabled}}"></textarea>
  </view>
  <button class="submit-wrapper" bindtap="submit">确认提交</button> 
</view>
<custom-list items="{{revenueTypes}}"></custom-list>